{
  "rules": {
    "//": "Cocina SAET · reglas de Firebase Realtime Database.",
    "//1": "Todo cerrado por defecto. Solo usuarios autenticados pueden acceder a 'cocina'.",
    "//2": "Crear los usuarios de Firebase Authentication antes de publicar estas reglas.",
    "//3": "El robot usa su cuenta técnica mediante secretos de GitHub Actions.",
    "//4": "Ver SECURITY_MIGRATION.md para aplicar el cambio sin interrumpir la app.",

    ".read": false,
    ".write": false,

    "cocina": {
      ".read": "auth != null",
      ".write": "auth != null",

      "doc": {
        ".validate": "newData.hasChildren()",
        "$campo": { ".validate": true }
      },

      "done": {
        "$tarea": { ".validate": "newData.isBoolean()" }
      },

      "brkDone": {
        "$tarea": { ".validate": "newData.isBoolean()" }
      },

      "$otro": { ".validate": false }
    }
  }
}
